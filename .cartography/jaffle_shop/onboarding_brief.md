# FDE Day-One Onboarding Brief

> Generated by The Brownfield Cartographer on 2026-03-13 21:52 UTC

---

## What is the primary data ingestion path?

The primary data ingestion path flows from raw sources (raw_customers, raw_orders, raw_payments) into the staging layer, where data is initially standardized. This ingestion is handled by the staging models which act as the entry point for all downstream transformations. Evidence: models/staging/stg_customers.sql, models/staging/stg_orders.sql, models/staging/stg_payments.sql.

---

## What are the 3–5 most critical output datasets/endpoints?

The most critical output datasets are the 'customers' and 'orders' models, which are identified as the project's final data sinks. These models aggregate business value and serve as the endpoints for downstream analytics and reporting. Evidence: models/customers.sql, models/orders.sql.

---

## What is the blast radius if the most critical module fails?

If the 'models/orders.sql' module fails, the blast radius would encompass the 'orders' data sink and potentially the 'customers' model, which relies on order history for lifetime value calculations. This failure would sever the data supply chain for financial reconciliation and marketing segmentation. Evidence: models/orders.sql, models/customers.sql.

---

## Where is the business logic concentrated vs. distributed?

Business logic is concentrated in the final transformation models ('customers.sql' and 'orders.sql'), which perform complex aggregations and joins, while the staging layer handles distributed, lightweight standardization. This architecture ensures that heavy computational logic is centralized near the output layer. Evidence: models/customers.sql, models/orders.sql, models/staging/stg_customers.sql.

---

## What has changed most frequently in the last 90 days (git velocity)?

The analysis indicates that no files have been identified as high velocity, suggesting the codebase has been stable or static recently. The surveyor results explicitly list an empty array for high velocity files, confirming a lack of recent rapid changes. Evidence: surveyor -> high_velocity_files: [].

---

## Supplementary Metrics

| Metric | Value |
|--------|-------|
| Total modules | 8 |
| Total functions | 0 |
| Total datasets | 5 |
| Total transformations | 5 |
| Circular dependencies | 0 |
| Graph nodes | 21 |
| Graph edges | 18 |

### Data Entry Points
- `raw_customers`
- `raw_orders`
- `raw_payments`

### Data Exit Points
- `customers`
- `orders`
