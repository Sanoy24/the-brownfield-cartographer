# FDE Day-One Onboarding Brief

> Generated by The Brownfield Cartographer on 2026-03-13 21:45 UTC

---

## What is the primary data ingestion path?

The primary data ingestion path flows from three raw sources (raw_customers, raw_orders, raw_payments) into corresponding staging models located in the 'models/staging' directory. These staging modules perform initial cleansing and standardization before passing data downstream. Evidence: models/staging/stg_customers.sql, models/staging/stg_orders.sql, models/staging/stg_payments.sql.

---

## What are the 3–5 most critical output datasets/endpoints?

The most critical output datasets are the 'customers' and 'orders' models, which are identified as the project's only data sinks. These models contain the final aggregated business logic and KPIs intended for end-user consumption. Evidence: models/customers.sql, models/orders.sql.

---

## What is the blast radius if the most critical module fails?

If 'models/staging/stg_payments.sql' fails, the blast radius would extend to both downstream models, 'orders' and 'customers', breaking the entire pipeline. This single point of failure in the payments data flow would sever critical financial reporting and customer lifetime value calculations. Evidence: models/staging/stg_payments.sql, models/orders.sql, models/customers.sql.

---

## Where is the business logic concentrated vs. distributed?

Business logic is concentrated in the 'customers.sql' and 'orders.sql' models, which handle complex aggregations like customer lifetime value and payment method breakdowns. In contrast, the logic in the 'staging' directory is distributed and limited to simple data cleansing and standardization tasks. Evidence: models/customers.sql, models/orders.sql, models/staging/stg_customers.sql.

---

## What has changed most frequently in the last 90 days (git velocity)?

There have been no high-velocity changes in the last 90 days, as the surveyor analysis explicitly returns an empty list for 'high_velocity_files'. This indicates a stable codebase with no recent active development hotspots or frequent refactoring. Evidence: surveyor -> high_velocity_files.

---

## Supplementary Metrics

| Metric | Value |
|--------|-------|
| Total modules | 8 |
| Total functions | 0 |
| Total datasets | 5 |
| Total transformations | 5 |
| Circular dependencies | 0 |
| Graph nodes | 21 |
| Graph edges | 18 |

### Data Entry Points
- `raw_customers`
- `raw_orders`
- `raw_payments`

### Data Exit Points
- `customers`
- `orders`
